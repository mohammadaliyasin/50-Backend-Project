// Not needed here because we are using exertnal api for data not db

// const mongoose = require('mongoose');

// const weatherSchema = new mongoose.Schema({
//     city: String,
//     temperature: Number,
//     weather: String,
//     humidity: Number
// }, { timestamps: true });

// module.exports = mongoose.model('Weather', weatherSchema);